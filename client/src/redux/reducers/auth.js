const initalState = {
  token: null,
  user: null,
};

const authReducer = (state = initalState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default authReducer;